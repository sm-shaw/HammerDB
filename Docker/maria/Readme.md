# MariaDB image

This HammerDB 6.0 Ubuntu 24.04 image uses the distribution's default architecture-correct `libmariadb3` runtime package, not an external MariaDB repository. Validation reports the package's actual version and checks the runtime library and HammerDB extension.
